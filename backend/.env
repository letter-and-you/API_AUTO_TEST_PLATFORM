# 允许访问的主机列表（逗号分隔）
ALLOWED_HOSTS=localhost,127.0.0.1,192.168.1.100

# 允许跨域的前端域名列表（逗号分隔）
CORS_ORIGINS=http://localhost:8080,http://127.0.0.1:8080

# Celery 消息队列和结果后端地址
CELERY_BROKER=redis://localhost:6379/0
CELERY_BACKEND=redis://localhost:6379/0

# 邮件服务配置（如使用SMTP）
EMAIL_HOST=smtp.qq.com
EMAIL_PORT=465
EMAIL_HOST_USER=3277990392@qq.com 
EMAIL_HOST_PASSWORD=vdmmgdcxvofadaha

# 告警通知 - SendGrid邮件配置（可选）
SENDGRID_API_KEY=  
# SendGrid的API密钥
FROM_EMAIL=alarm@api-test.com  
# 告警邮件发件人

# 告警通知 - Twilio短信配置（可选）
TWILIO_ACCOUNT_SID= 
# Twilio账户SID
TWILIO_AUTH_TOKEN=  
# Twilio认证令牌
TWILIO_PHONE_NUMBER=  
# Twilio电话号码

# 测试请求超时时间（秒）
TEST_REQUEST_TIMEOUT=30